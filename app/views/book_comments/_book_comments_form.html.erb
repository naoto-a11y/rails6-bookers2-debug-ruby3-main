<%= form_with model: [book, bookcomment], local: false do |f| %>
  <div>
  <%= f.label :comment %>
  <%= f.text_area :comment, class: 'form-control',  id: "comment_form"%>
  <%= f.submit %>
  </div>
<% end %>